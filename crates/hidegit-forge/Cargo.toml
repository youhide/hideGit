[package]
name = "hidegit-forge"
description = "Forge trait and pull request alerts for hideGit. GitHub implementation lands in M4."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# M1 ships this crate as a compiling stub: the `Forge` trait and its
# provider-neutral data model, with no implementation and no network.
# `octocrab`, `keyring` and the polling subscription arrive in M4.
[dependencies]
hidegit-core.workspace = true
thiserror.workspace = true
time.workspace = true

[lints]
workspace = true
