[package]
name = "hidegit-forge"
description = "Forge trait and pull request alerts for hideGit. GitHub implementation lands in M4."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
hidegit-core.workspace = true
# The UI holds a `dyn Forge`, and native `async fn` in a trait is not
# dyn-compatible. See the trait's own note in `lib.rs`.
async-trait = "0.1"
thiserror.workspace = true
time.workspace = true

[lints]
workspace = true
